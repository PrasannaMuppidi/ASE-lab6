package com.assignment.lab;

public class VolumeChecker {
public double Volcheck(double length) {
		
		double vol =  (length * length * length);
		System.out.println("values: " +vol  + " + " + length);
		return vol;

}
}